name: Deploy to GitHub Pages

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build_site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Log BASE_PATH
        run: |
          echo "BASE_PATH: /${{ github.event.repository.name }}"

      - name: Build site
        env:
          BASE_PATH: '/${{ github.event.repository.name }}'
        run: |
          echo "Starting build process..."
          npm run build
          echo "Build completed."

      - name: Debug Build Artifacts
        run: |
          echo "Build directory structure:"
          ls -R build

      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'build/' # Make sure this matches your SvelteKit output directory

  deploy:
    needs: build_site
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write

    steps:
      - name: Debug Uploaded Artifacts
        run: echo "Artifacts uploaded for deployment."

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
